{
  "name": "json-api",
  "version": "1.0.0",
  "description": "JSON Server API for React app",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch Data/dummyData.json --port 10000 --static ./Data"
  },
  "dependencies": {
    "json-server": "^1.0.0"
  }
}
