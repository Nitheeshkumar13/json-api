{
  "name": "json-api",
  "version": "1.0.0",
  "description": "JSON Server API for React app",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch dummyData.json --port $PORT --host 0.0.0.0 --static ./"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  },
  "engines": {
    "node": "18.x"
  }
}
